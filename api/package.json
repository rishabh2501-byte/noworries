{
  "name": "ui-validator-api",
  "version": "1.0.0",
  "description": "API Server for UI Validator - Locator Extraction",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "npx tsx index.ts",
    "build": "echo 'No build step required - API is server-side'",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "playwright": "^1.41.2"
  },
  "engines": {
    "node": ">=18"
  }
}
